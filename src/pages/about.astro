---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="About - Your Name">
  <section class="page-header">
    <div class="container">
      <h1>About</h1>
    </div>
  </section>

  <section class="content">
    <div class="container">
      <div class="grid">
        <aside class="sidebar">
          <div class="photo"></div>
          <div class="quick-info">
            <p><strong>Location</strong><br/>San Francisco, CA</p>
            <p><strong>Focus</strong><br/>Product, Startups</p>
          </div>
        </aside>
        
        <div class="main">
          <div class="prose">
            <p class="lead">
              I'm a startup founder and product builder with a passion for creating 
              things that matter.
            </p>
            
            <p>
              Over the past 8+ years, I've founded multiple companies, built products 
              used by thousands, and learned more from failures than successes. I believe 
              the best products come from understanding people deeply and shipping relentlessly.
            </p>
            
            <p>
              Currently, I'm focused on building the next big thing. When I'm not working, 
              you'll find me reading, exploring new places, or obsessing over details that 
              probably don't matter.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <div class="section-header">
        <h2>Experience</h2>
        <span class="section-line"></span>
      </div>
      
      <div class="timeline stagger">
        <div class="timeline-item">
          <div class="timeline-date">2024 —</div>
          <div class="timeline-content">
            <h3>Building Something New</h3>
            <p>Stealth mode. Working on the future of technology.</p>
          </div>
        </div>
        
        <div class="timeline-item">
          <div class="timeline-date">2021 — 24</div>
          <div class="timeline-content">
            <h3>Second Venture</h3>
            <p>Co-founded and scaled a B2B SaaS company. Successfully exited.</p>
          </div>
        </div>
        
        <div class="timeline-item">
          <div class="timeline-date">2018 — 21</div>
          <div class="timeline-content">
            <h3>First Startup</h3>
            <p>Built my first product from scratch. Made every mistake possible.</p>
          </div>
        </div>
        
        <div class="timeline-item">
          <div class="timeline-date">2015 — 18</div>
          <div class="timeline-content">
            <h3>The Beginning</h3>
            <p>Started as a developer, quickly realized I wanted to build entire products.</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="section section-alt" id="contact">
    <div class="container">
      <div class="contact-grid">
        <div class="contact-intro">
          <h2>Let's talk</h2>
          <p>
            Whether you're working on something interesting, looking for advice, 
            or just want to say hi—I'd love to hear from you.
          </p>
        </div>
        
        <div class="contact-links">
          <a href="mailto:hello@yourname.com" class="contact-item">
            <span class="contact-label">Email</span>
            <span class="contact-value">hello@yourname.com</span>
          </a>
          
          <a href="https://twitter.com/yourhandle" target="_blank" rel="noopener" class="contact-item">
            <span class="contact-label">Twitter</span>
            <span class="contact-value">@yourhandle</span>
          </a>
          
          <a href="https://cal.com/yourhandle" target="_blank" rel="noopener" class="contact-item">
            <span class="contact-label">Schedule</span>
            <span class="contact-value">Book a call</span>
          </a>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .page-header {
    padding: var(--space-4xl) 0 var(--space-2xl);
  }
  
  .page-header h1 {
    font-size: var(--text-5xl);
    animation: fadeUp var(--duration-slow) var(--ease) forwards;
  }
  
  /* Content Grid */
  .content {
    padding-bottom: var(--space-4xl);
  }
  
  .grid {
    display: grid;
    grid-template-columns: 220px 1fr;
    gap: var(--space-4xl);
    align-items: start;
  }
  
  .sidebar {
    position: sticky;
    top: calc(var(--nav-height) + var(--space-xl));
  }
  
  .photo {
    aspect-ratio: 1;
    background: var(--color-surface);
    border-radius: 8px;
    margin-bottom: var(--space-xl);
  }
  
  .quick-info p {
    font-size: var(--text-sm);
    margin-bottom: var(--space-md);
    line-height: 1.5;
  }
  
  .quick-info strong {
    color: var(--color-text-muted);
    font-weight: 500;
    font-size: var(--text-xs);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }
  
  .main .prose {
    max-width: 520px;
  }
  
  .main .lead {
    font-size: var(--text-xl);
    color: var(--color-text);
    line-height: 1.6;
    margin-bottom: var(--space-xl);
  }
  
  /* Section */
  .section {
    padding: var(--space-4xl) 0;
  }
  
  .section-alt {
    background: var(--color-surface);
  }
  
  .section-header {
    display: flex;
    align-items: center;
    margin-bottom: var(--space-2xl);
  }
  
  .section-header h2 {
    font-size: var(--text-sm);
    font-family: var(--font-sans);
    font-weight: 500;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--color-text-muted);
  }
  
  .section-line {
    flex: 1;
    height: 1px;
    background: var(--color-border);
    margin-left: var(--space-xl);
  }
  
  /* Timeline */
  .timeline {
    max-width: 600px;
  }
  
  .timeline-item {
    display: grid;
    grid-template-columns: 100px 1fr;
    gap: var(--space-xl);
    padding: var(--space-lg) 0;
    border-bottom: 1px solid var(--color-border);
  }
  
  .timeline-item:first-child {
    border-top: 1px solid var(--color-border);
  }
  
  .timeline-date {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    padding-top: 2px;
  }
  
  .timeline-content h3 {
    font-size: var(--text-lg);
    font-family: var(--font-sans);
    font-weight: 500;
    margin-bottom: var(--space-xs);
  }
  
  .timeline-content p {
    font-size: var(--text-sm);
    margin: 0;
    line-height: 1.6;
  }
  
  /* Contact */
  .contact-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--space-4xl);
    align-items: start;
  }
  
  .contact-intro h2 {
    font-size: var(--text-3xl);
    margin-bottom: var(--space-md);
  }
  
  .contact-intro p {
    max-width: 360px;
  }
  
  .contact-links {
    display: flex;
    flex-direction: column;
  }
  
  .contact-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--space-lg) 0;
    border-bottom: 1px solid var(--color-border);
    transition: all var(--duration) var(--ease);
  }
  
  .contact-item:first-child {
    border-top: 1px solid var(--color-border);
  }
  
  .contact-item:hover {
    padding-left: var(--space-md);
  }
  
  .contact-label {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
  }
  
  .contact-value {
    font-size: var(--text-base);
    color: var(--color-text);
    transition: color var(--duration-fast) var(--ease);
  }
  
  .contact-item:hover .contact-value {
    color: var(--color-text-secondary);
  }
  
  /* Mobile */
  @media (max-width: 768px) {
    .grid {
      grid-template-columns: 1fr;
      gap: var(--space-2xl);
    }
    
    .sidebar {
      position: static;
      display: flex;
      gap: var(--space-xl);
      align-items: flex-start;
    }
    
    .photo {
      width: 120px;
      flex-shrink: 0;
      margin-bottom: 0;
    }
    
    .timeline-item {
      grid-template-columns: 1fr;
      gap: var(--space-sm);
    }
    
    .contact-grid {
      grid-template-columns: 1fr;
      gap: var(--space-2xl);
    }
  }
  
  @keyframes fadeUp {
    from { 
      opacity: 0; 
      transform: translateY(20px); 
    }
    to { 
      opacity: 1; 
      transform: translateY(0); 
    }
  }
</style>
